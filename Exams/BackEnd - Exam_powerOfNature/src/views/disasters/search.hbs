<section class="event-search">
    <h1 id="title">Search</h1>

    <form class="search-form" method="GET">
        <input type="text" class="search-event" name="disasterName" placeholder="Search by Name...">
        <select id="type" name="disasterType">
            <option value="Wildfire">Wildfire</option>
            <option value="Flood">Flood</option>
            <option value="Earthquake">Earthquake</option>
            <option value="Hurricane">Hurricane</option>
            <option value="Drought">Drought</option>
            <option value="Tsunami">Tsunami</option>
            <option value="Other">Other</option>
        </select>
        <button type="submit" class="btn-search">Search</button>
    </form>

    <div class="search-result">
        {{#if disasters}}
        <div class="search-list">
            {{#each disasters}}
            <div class="event">
                <div class="img">
                    <img src="{{image}}">
                </div>
                <div class="event-info">
                    <h1>{{disasterName}}</h1>
                    <p><span>Location: </span>{{location}}</p>
                    <p><span>Type: </span>{{disasterType}}</p>
                </div>

                <a href="/disasters/{{this._id}}/details" class="btn-details">Details</a>
            </div>
            {{/each}}
        </div>
        {{else}}
        <div class="no-match">
            <p>No match was found!</p>
        </div>
        {{/if}}

    </div>